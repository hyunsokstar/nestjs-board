import {Board as BoardEntity} from "../database/board.entity"


export class BoardRepository {
    // constructor(
    //     @InjectRepository(BoardEntity)
    //     private readonly boardRepository: Repository(BoardEntity)
    // )
}